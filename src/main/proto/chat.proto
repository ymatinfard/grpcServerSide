syntax = "proto3";

option java_package = "com.matin.happychat";
option java_multiple_files = true;

message ChatMessage {
  string sender = 1;
  string message = 2;
}

message Empty {}

service ChatService {
  rpc ChatStream(stream ChatMessage) returns (stream ChatMessage);
}
